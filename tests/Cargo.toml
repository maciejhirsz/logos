[[bench]]
harness = false
name = "bench"

[dependencies]
logos-derive2 = {path = "../logos-derive"}
logos2 = {path = "../logos", default-features = false, features = ["std"]}

[dev-dependencies]
criterion = "0.4"

[package]
name = "tests"
publish = false
version = "0.0.0"
