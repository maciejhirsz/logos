[package]
name = "logos-fuzz"
edition = "2021"
publish = false

[dependencies]
afl = "0.15"
arbitrary = "1.3"
logos-codegen = { path = "../logos-codegen", features = ["fuzzing"] }

[package.metadata.release]
shared-version = true
